name: Publicacion Blog

on:
  schedule:
    - cron: "0 12 * * 1"   # Lun 09:00 Peru — Analisis Semanal
    - cron: "0 12 * * 2"   # Mar 09:00 Peru — Comparativa de Tasas
    - cron: "0 12 * * 3"   # Mie 09:00 Peru — Pulso Media Semana
    - cron: "0 12 * * 4"   # Jue 09:00 Peru — Analisis de Actualidad
    - cron: "0 12 * * 5"   # Vie 09:00 Peru — Analisis de Actualidad
    - cron: "0 12 * * 6"   # Sab 09:00 Peru — Articulo Educativo
    - cron: "0 12 * * 0"   # Dom 09:00 Peru — Articulo Educativo
  workflow_dispatch:

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm ci
      - run: npm start
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          OPENAI_MODEL: gpt-5.4-mini
